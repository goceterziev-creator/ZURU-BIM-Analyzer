"""DWG converter stub removed from production.

This module previously provided a test-only fake converter. To avoid accidental
use in production, importing this module now raises ImportError. Test-only
stubs live under the tests/ package.
"""

raise ImportError("dwg_converter removed from production; use tests.dwg_converter for test-only stub")
